<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<link rel="stylesheet" type="text/css" href="/style.css">
<title>Edit Character</title>
</head>
<body>
<h1>Edit Character</h1>
<form action="/users/update/<%= user.id %>" method="POST">
    
    <label for = "first_name">First Name:</label>
    <input type = "text" id="firstNameInput" name = "first_name" value="<%= user.first_name %>" required><br>
    <br>
    <label for = "last_name"> Last Name:</label>
    <input type = "text" id = "lastNameInput" name = "last_name" value="<%= user.last_name %>" required><br>
    <br>
    <label for="age">Age:</label>
    <input type="number" id = "ageInput" name="age" value="<%= user.age %>" required><br>
    <br>
    <label for = "race"> Race:</label>
    <input type = "text" id = "raceInput" name = "race" value="<%= user.race %>" required><br>
    <br> 
    <label for = "dndclass"> D&D Class:</label>
    <input type = "text" id = "dndClassInput" name = "dndclass" value="<%= user.dndclass %>" required><br>
    <br>
    <label for = "specialization"> Specialization:</label>
    <input type = "text" id = "specializationInput" name = "specialization" value="<%= user.specialization %>" required><br>
    <br>   
    <button type="submit">Update Character</button>
</form>
<script>
    document.getElementById('firstNameInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[0-9]/g, ''); // Remove any numbers
    });
    document.getElementById('lastNameInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[0-9]/g, ''); // Remove any numbers
    });
    document.getElementById('ageInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[^0-9]/g, ''); // Allow only numbers
    });
    document.getElementById('raceInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[0-9]/g, ''); // Remove any numbers
    });
    document.getElementById('dndClassInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[0-9]/g, ''); // Remove any numbers
    });
    document.getElementById('specializationInput').addEventListener('input', function(e) {
        this.value = this.value.replace(/[0-9]/g, ''); // Remove any numbers
    });
</script>


</body>
</html>

